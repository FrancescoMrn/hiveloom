schema_version: 1
harness: .
extensions:
  - eval_extension.py
dataset:
  loader: signal_lab_cases
  include_expected_in_input: false
scorers:
  - signal_lab_amount
repetitions: 1
# The executor is a local scripted provider, so there is no served snapshot to
# pin; record what it reports.
model_identity: warn
