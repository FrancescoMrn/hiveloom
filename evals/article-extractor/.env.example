# Required for the Claude arms (haiku-harness, haiku-raw, sonnet-raw)
ANTHROPIC_API_KEY=

# Optional: explicit path to the hiveloom CLI. Defaults to running
# `uv run --project ../.. hiveloom` from this directory.
HIVELOOM_BIN=
