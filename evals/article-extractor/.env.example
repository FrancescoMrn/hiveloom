# Required for the Claude arms (haiku-harness, haiku-raw, sonnet-raw)
ANTHROPIC_API_KEY=

# Optional: explicit path to the hiveloom CLI. Defaults to the repo's own
# ../../.venv/bin/hiveloom when it exists, else whatever `hiveloom` is on PATH.
HIVELOOM_BIN=
